using UnityEngine;

public class Introduction : MonoBehaviour
{
    [SerializeField] Texture2D[] horizontalKeys;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
